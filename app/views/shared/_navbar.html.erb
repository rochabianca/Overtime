<% if current_user %>
  <div class="grid">
    <nav class="navbar">
      <div class="flex flex__align--center">
        <div class="navbar__brand">Overtime</div>
        <div class="navbar__menu">
          <%= link_to "Home", root_path, class: active?(root_path) %>
          <%= link_to "Time Entries", posts_path, class: active?(posts_path) %>
          <%= link_to "New Entry", new_post_path, id: 'new_post_from_nav', class: active?(new_post_path) %>
        </div>
      </div>

      <div>
        <a href="#" class=""><%= current_user.first_name %> | <%= link_to "Admin |", admin_root_path if current_user.try(:type) == 'AdminUser' %>  <%= link_to "Log Out", destroy_user_session_path, method: :delete %></i></a>
      </div>
    </nav>
  </div>
<% end %>